{
  "publisher": "xiaochi",
  "name": "llamachat",
  "displayName": "llamaChat",
  "description": "chat with llama, or copy files' name and content",
  "version": "0.0.4",
  "repository": "https://github.com/picasso250/vscode-copyfiles",
  "engines": {
    "vscode": "^1.88.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "menus": {
      "explorer/context": [
        {
          "command": "copyfiles.copyFileNamesAndContent"
        }
      ]
    },
    "commands": [
      {
        "command": "copyfiles.copyFileNamesAndContent",
        "title": "Copy File Name And Content",
        "category": "Explorer"
      },
      {
        "command": "copyfiles.copyOneFile",
        "title": "Copy This File Name And Content"
      },
      {
        "command": "copyfiles.copySelectedText",
        "title": "Copy Selected Text"
      },
      {
        "command": "copyfiles.openPanel",
        "title": "Open AI Chat Panel"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.88.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "18.x",
    "@typescript-eslint/eslint-plugin": "^7.4.0",
    "@typescript-eslint/parser": "^7.4.0",
    "eslint": "^8.57.0",
    "typescript": "^5.3.3",
    "@vscode/test-cli": "^0.0.8",
    "@vscode/test-electron": "^2.3.9"
  }
}
